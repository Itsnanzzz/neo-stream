interface:
  display_name: "Neo Stream"
  short_description: "Repo-specific patterns and workflows for neo-stream"
  default_prompt: "Use the neo-stream repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true